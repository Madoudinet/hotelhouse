{% extends 'base.html.twig' %}

{% block title %}Nos chambres{% endblock %}

{% block body %}

<h1 class="text-center my-4">Nos chambres</h1>

<div class="container">
<section class="row justify-content-around">
{% for chambre in chambres %}
<div class="mb-3 col-3">
  <h3 class="card-header my-3">{{ chambre.titre }}</h3>
<img src="{{ asset('img/upload/'~ chambre.photo) }}" class="d-block w-100" alt="...">
  <ul class="list-group list-group-flush">
    <li class="list-group-item"><span class="text-dark-50">Prix par nuit : </span>{{ chambre.prixJournalier }} €</li>
    <li class="list-group-item">{{ chambre.descriptionCourte }}</li>
  </ul>
  <div class="card-body text-center mt-3">
    <a href="{{ path('app_chambre_commande', {id: chambre.id}) }}" class="btn btn-primary">Voir plus</a>
  </div>
</div>

    
{% endfor %}
</section>
</div>

{% endblock %}